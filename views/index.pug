extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    each message in messages
      li
        span.username= message.user
        span.date= message.added.toDateString()
        p= message.text
